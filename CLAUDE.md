# Minecraft 标签锻造台 —— 项目须知

详细的已验证事实、坑、待办在 `.claude/skills/mc-tag-forge/SKILL.md`，改代码前读。

## 用户是谁

ZONZON，**不是开发者**。会用工具、有判断力、能验收效果，但**不读代码**。

所以：

- **不要让他看 diff。** 给他看 diff 等于什么都没给。
- **不要用「重构了 `renderRuns`」这类说法。** 他不知道那是什么。说「段落那一栏改成一行一段了」。
- **不要假设他知道怎么验证。** 每次都要具体告诉他点哪里、看什么。

## 每次改完，回复必须包含这四段

**1｜改了什么**
用他的语言说，不是用函数名。一到三句。
> 包裹从「独立开一对颜色标签」改成「并进首尾段的文字里」，所以一对 `「」` 从 30 格降到 2 格。

**2｜怎么验证**
具体到操作步骤，不要说「看一下效果」。
> 打开 Live Preview → 滚到「包裹」那一栏 → 点「角括号」→ 看「输出」栏的长度计。

**3｜预期看到什么**
给出具体的数字或文字，让他能对照。
> 长度应该从 18 变成 20（+2）。改之前是 48（+30）。

**4｜测试结果**
跑 `node tests/run-all.js`，把通过/失败数贴出来。有失败就不要说「改好了」。
> 4 个测试文件、114 条断言全绿。

## 协作方式

- **动手前先给选项 + 推荐哪个 + 为什么。** 不要擅自替他决定设计问题。
- **UI 类改动给变体文件**（`variant-a.html` / `b` / `c`），让他在浏览器里点着比，比文字描述可靠得多。选定后合并进主文件、删掉变体。
- **先诊断根因再改。** 他报的现象常常不是根因（「点不到花体」实际是花体做成了破坏性改写；「删不掉段落」实际是删除键被挤到换行外）。
- **不要猜 UI 路径。** 不确定某个按钮在哪、某个命令叫什么，就说不确定并让他截图，不要编一个看起来合理的路径。
- **不要用省事的替代方案。** 他要的是对的结果，宁可多花一轮。
- 他会发游戏内截图当证据。放大读像素是有效手段，用它。

## 硬性规则

1. 改完**必须**跑 `node tests/run-all.js`，全绿才算完成。这套测试抓到过手打映射表的错字、hover 污染输出、包裹成本算错 15 倍——全部是看代码看不出来的。
2. 长度、色值、字符数**一律用代码算**，不手算。手算已经错过两次。
3. 连续 Unicode 区块的映射表**必须** `seq(起始码点)` 生成，不能手打。
4. 同一份 HTML 会跑在两种环境（GitHub Pages 托管 / Claude artifact 沙箱）。artifact 里 `localStorage` 和部分 `console` 方法会抛错，所以这类调用一律 try/catch 降级。
5. 交付 `mc-tag-forge.html` 时，回复最后一行写「这是第 N 次 update」，N 取 SKILL.md 里的计数器加一，并同步更新那个计数器。交付其他文件不写。
6. 没改动的文件不要重新交付。

## 提交推送由用户决定

不要自动 `git commit` 或 `git push`。改完告诉他怎么验证，让他自己决定推不推。
